extends ./_layout/_page.pug

block vars
  -
    var page = data.pages.game;

block content
  include ./_modules/_preloader.pug
  .l-page-navbar(style='    \
  z-index: 2;\
  position: absolute;\
  display: flex;\
  justify-content: space-around;\
  width: 100%;\
  font-size: 30px;\
  opacity: 0.6;\
  margin-top: 50px;')
    each item in data.pages
      a(href=`${item.path}`) #{item.title}
  canvas(id="canvas-webgl", class="p-canvas-webgl")
